services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-dev-secret-change-me}
    command: python -m flask --app main.py run --host 0.0.0.0 --port 8000
